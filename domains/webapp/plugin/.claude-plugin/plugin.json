{
  "name": "xenomoon-webapp",
  "description": "Web app domain package for React + Node.js (tRPC / Lambda / Drizzle). Ships an issue-driven, human-gated pipeline — bug-triage → senior-dev (solution) → developer (implement) — with /feedback /triage /solution /implement /build commands. Adapted for LexFlow.",
  "version": "0.1.0",
  "author": { "name": "Xenomoon Forge" }
}
